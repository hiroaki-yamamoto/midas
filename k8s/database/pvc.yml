apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  storageClassName: midas
  resources:
    requests:
      storage: 1Gi
    limits:
      storage: 100Gi
