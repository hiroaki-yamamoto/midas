type: struct
rust:
  derive:
    - Clone
    - PartialEq
    - "::serde::Serialize"
    - "::serde::Deserialize"
  attrs:
    - 'serde(rename_all = "camelCase")'
members:
  exchange: { use: Exchanges }
  symbol: string
  start: string
  end: string
