type: struct
rust:
  derive:
    [Debug, Clone, PartialEq, "::serde::Serialize", "::serde::Deserialize"]
  attrs: ['serde(rename_all = "camelCase")']
typescript:
  rename: camelCase
members:
  secs: i64
  nanos: u32
