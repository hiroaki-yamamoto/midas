type: struct
rust:
  derive:
    [Debug, Clone, PartialEq, "::serde::Serialize", "::serde::Deserialize"]
  attrs: ['serde(rename_all = "camelCase")']
attrs: ['serde(rename_all = "camelCase")']
typescript:
  rename: camelCase
members:
  pagination:
    use: Pagination
  demo_mode:
    type: bool
    rust:
      attrs: ["cfg(debug_assertions)"]
