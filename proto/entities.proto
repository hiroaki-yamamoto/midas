syntax = "proto3";

package entities;
option go_package = "rpc";

enum Exchanges {
  Binance = 0;
}

message SymbolInfo {
  string symbol = 1;
  string status = 2;
  string base = 3;
  string quote = 4;
}
